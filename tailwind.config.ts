/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{ts,tsx}"],
  darkMode: "class",
  // Theme configuration moved to CSS in globals.css using @theme directive
  // Custom utilities moved to CSS using @utility directive
};
